{
  "name": "singleton-deployer",
  "description": "Utils to use singleton factories to deploy a contract",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn && yarn workspaces run build",
    "test": "yarn build && yarn workspace @gnosis.pm/singleton-deployer-example test"
  },
  "workspaces": {
    "packages": [
      "packages/core",
      "packages/provider/*",
      "packages/factory/*",
      "packages/defaults/*",
      "packages/example"
    ]
  }
}
